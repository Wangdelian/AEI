package io.jeasyframework.utils;

public class ArrayUtils {

    /**
     *
     * @param arr
     * @return
     */
    public static int calArraySum(Integer[] arr) {
        int sum = 0;
        for (Integer item : arr) {
            sum += item == null ? 0 : item;
        }

        return sum;
    }
}
